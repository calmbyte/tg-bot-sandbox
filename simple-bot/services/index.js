const { whoami } = require('./whoami');
const { greetings } = require('./greetings');
const { guessNumber } = require('./guess-number');
const { unrecognized } = require('./unrecognized');

module.exports = {
  whoami,
  greetings,
  guessNumber,
  unrecognized,
};